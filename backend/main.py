# Stub — this app is a static React frontend served via Dockerfile (serve -s dist)
# The deploy tool uses this file to detect the project root
print('NexusChat frontend — see Dockerfile for runtime')
